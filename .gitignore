# 依赖
node_modules/

# Electron 打包产物（electron-builder 输出）
dist/
out/

# 构建产物不入库：由 agent-team-server / agent-team-web 构建后同步（见 README）
resources/server/*
!resources/server/.gitkeep
resources/web/*
!resources/web/.gitkeep
resources/jre/
# 托盘图标以 build/icon.ico 为唯一来源（开发态直接读 build/，打包态经 extraResources 复制）
resources/icon.ico

# 日志
*.log
.idea
